# Arquivo usado no dev local (quando rodar `npm start` localmente).
# Em PRODUÇÃO (Vercel), defina REACT_APP_BACKEND_URL no painel de env variables.
REACT_APP_BACKEND_URL=http://localhost:8001
GENERATE_SOURCEMAP=false
CI=false
